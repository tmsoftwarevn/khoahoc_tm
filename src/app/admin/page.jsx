"use client";

import { useRouter } from "next/navigation";
import { useEffect } from "react";

const page = () => {
    // const route = useRouter();

    // useEffect(() => {
    //     route.push('/admin/banner');
    // }, [])

    return (
        <div>
            this is admin
        </div>
    );
}

export default page;